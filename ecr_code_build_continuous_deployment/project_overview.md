# ECR Code Build Continuous Deployment Project

## Overview
This project demonstrates the building of a Continuous Integration/Deployment (CI/CD) pipeline that automatically deploys a Docker image to ECR when a pull request is made to a git repository containing a Dockerfile. The Docker image can then be used to create a Lambda function that executes a Python script.